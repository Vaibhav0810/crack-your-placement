 void deleteNode(Node *del)
    {
       // Your code here
       if(del==NULL){
           return;
       }
       
      del->data=del->next->data;
      del->next=del->next->next;
      
      
    }
